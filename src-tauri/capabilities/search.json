{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "search",
  "description": "Search window with event communication only; document operations stay in the main window",
  "windows": ["search"],
  "permissions": ["core:event:default"]
}
