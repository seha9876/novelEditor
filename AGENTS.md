# novelEditor 固有の指示

コード変更を伴うタスクでは、変更の規模にかかわらず、実装と必要な検証を `implementer` サブエージェントに委任してください。

実装後は、実装担当とは独立した `reviewer` サブエージェントに変更内容のレビューを依頼してください。主担当は指摘の妥当性を判断し、必要な修正を `implementer` に委任したうえで、結果を最終確認してください。

調査や質問への回答など、コード変更を伴わないタスクでは委任を必須としません。
